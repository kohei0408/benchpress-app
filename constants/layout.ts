import type { ViewStyle } from "react-native";

export const screenFlex: ViewStyle = { flex: 1 };

export const scrollContent: ViewStyle = {
  paddingHorizontal: 20,
  paddingTop: 16,
  paddingBottom: 40,
};

export const modalScrollContent: ViewStyle = {
  paddingHorizontal: 20,
  paddingTop: 16,
  paddingBottom: 44,
};
